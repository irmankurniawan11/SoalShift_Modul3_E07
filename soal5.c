#include<stdio.h>
#include<pthread.h>
#include<string.h>
#include<unistd.h>

pthread_t tid1;
pthread_t tid2;

int main(int temp,char *argv[]){
pthread_t tid[isi];
for(int x=1;x<;x++){
pthread_create(&(tid[x],NULL,&fungsinya,(void *) argv[x]);
}

for(int x=1;x<;x++){
pthread_join(tid[x],NULL);
}

}
